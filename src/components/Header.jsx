/**
 * Header.jsx — Top Header & Navigation bar component alias.
 * Re-exports Navbar for flexible component usage.
 */
import Navbar from './Navbar';

export { Navbar as Header };
export default Navbar;
